version: '3'

services:
  sandbox:
    tty: true
    entrypoint: /home/root/start.sh
    image: ronsm/ralt-signalman-dashboard
    build: .
    volumes:
      - ./shared:/home/sandbox/shared
    ports:
      - 5000:5000
      - 80:80